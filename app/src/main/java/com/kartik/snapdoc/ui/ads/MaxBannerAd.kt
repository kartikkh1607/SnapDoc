package com.kartik.snapdoc.ui.ads

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * No-op stub kept so existing call sites compile. The app is fully free and
 * doesn't render any banner ads. Safe to delete once all call sites are gone.
 */
@Composable
@Suppress("UnusedParameter")
fun MaxBannerAd(modifier: Modifier = Modifier) {
    // Intentionally empty.
}
